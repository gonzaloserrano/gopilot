name: Skill Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  skill-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: thedaviddias/skill-check@v1.2.0
        with:
          path: .
          format: github
